import java.util.*;

/**
 * Problem: [Title] ([Difficulty])
 * Link: https://leetcode.com/problems/...
 *
 * Approach: one-line summary. Full explanation in README.md.
 * Time: O(?)
 * Space: O(?)
 */
class Solution {
    public int[] solve(int[] nums, int target) {
        // implementation
        return new int[0];
    }

    public static void main(String[] args) {
        Solution s = new Solution();
        // quick manual sanity check — keep lightweight, not a full test suite
        int[] result = s.solve(new int[]{2, 7, 11, 15}, 9);
        System.out.println(Arrays.toString(result));
    }
}
